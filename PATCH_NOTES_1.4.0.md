# RoiLingo v1.4.0

## 이번 버전의 핵심 수정

### 1. "번역은 화면에 보이는데 RoiLingo가 엉뚱한 문장을 가져오는" 문제

v1.3.0은 Papago 같은 사이트에서 실제 번역문 대신 언어 선택 메뉴를 DOM 후보로 선택할 수 있었습니다.

예:

```text
한국어 한국어 영어 일본어 중국어(간체) 중국어(번체) ...
```

이 값이 한 번 번역 캐시에 저장되면 이후 같은 OCR 원문은 웹사이트를 다시 읽지 않고 `(cache)` 결과를 재사용했기 때문에, 사이트 화면에는 올바른 번역이 있어도 프로그램에는 계속 잘못된 문자열이 표시될 수 있었습니다.

v1.4.0의 수정:

- `translation-cache-hybrid-v2.json`로 캐시 세대를 변경하여 이전 잘못된 캐시를 자동 격리
- **번역 캐시 비우기** 버튼 추가
- 언어 선택 메뉴 / 사이트 UI 문자열을 번역 결과로 인정하지 않는 품질 검사 추가
- 원문 DOM 요소의 위치를 찾은 뒤, 같은 높이의 오른쪽 번역 패널 텍스트를 우선 선택하는 **layout-anchor 추출** 추가
- 낮은 신뢰도의 DOM 결과는 바로 확정하지 않고 Copy-button/Clipboard 폴백까지 시도
- Copy 폴백은 임시 marker를 사용하여 클립보드에 이미 같은 문장이 있어도 결과를 감지
- 접근성 label이 없는 아이콘형 Copy 버튼에 대한 위치 기반 폴백 추가

실행 로그에서 정상적으로 읽으면 다음 중 하나가 표시됩니다.

```text
WEBREAD Papago Web: 결과 읽기 성공 (known-selector, ...)
WEBREAD Papago Web: 결과 읽기 성공 (layout-anchor, ...)
WEBREAD Papago Web: 결과 읽기 성공 (copy-button-clipboard, ...)
```

### 2. 게임 위 반투명 오버레이를 ROI별로 조절

사용자가 요청한 것은 별도 실시간 번역 창뿐 아니라 **게임 화면 위에 직접 보이는 반투명 클릭 통과 오버레이**의 조절 기능이었습니다.

`설정 / ROI → 게임 오버레이 위치/크기/투명도`에서 ROI별로 다음을 변경할 수 있습니다.

- X 위치 보정
- Y 위치 보정
- 너비 배율
- 배경 불투명도
- 번역 글자 크기
- OCR 원문 표시
- Provider / OCR 정확도 / 교차 일치도 표시

감시 중 변경하면 즉시 반영되며 자동 저장됩니다.

### 3. 별도 실시간 번역 창 재열기

버튼 이름을 **실시간 번역 창 열기/다시 열기**로 명확하게 변경했습니다.
사용자가 창을 닫아도 같은 버튼으로 다시 만들 수 있습니다.

## 호환성

- 기존 ROI 좌표와 Web/API 설정을 그대로 사용합니다.
- Settings schema를 5로 올렸으며 기존 ROI에는 오버레이 기본값을 자동 적용합니다.
- 이전 번역 기록(JSONL)은 삭제하지 않습니다.
- 이전 `translation-cache-hybrid-v1.json`은 새 버전에서 사용하지 않습니다.
