# RoiLingo 1.6.2

## 수정 사항

- `HybridBalanced`에서 API/로컬 Provider가 하나도 없으면 Papago/Google/DeepL Web을 하나만 회전 선택하지 않고, 활성화된 Web Provider를 모두 병렬 실행하도록 수정했습니다.
- Web 번역 요청마다 페이지를 새로 탐색하던 방식을 제거했습니다. 대상 언어가 바뀔 때만 번역 페이지를 준비하고, 이후 OCR 문장은 현재 페이지의 실제 입력칸에 직접 넣습니다.
- WebView2 입력을 DOM `value` 대입에만 의존하지 않고 Chromium DevTools `Input.insertText`를 사용하도록 보강했습니다. React/Vue/contenteditable 입력에서도 실제 키 입력에 가까운 이벤트 경로를 사용합니다.
- 번역 사이트의 SPA hydration으로 입력 문장이 사라지는 경우 첫 4초 동안 주기적으로 확인하고 자동 재입력합니다.
- 입력 자체가 실패하면 결과 대기 타임아웃까지 끌지 않고 `원문 입력칸에 OCR 문장을 넣지 못했습니다`라는 명확한 진단을 남깁니다.
- 캐시를 `translation-cache-hybrid-v4.json`으로 분리했습니다. Web만 사용하는 교차검증 모드에서는 캐시가 WebView 실행 자체를 건너뛰지 않도록 변경해, 활성화된 웹 탭에 실제 OCR 문장이 계속 전달됩니다.
- 메인 상단에 `오버레이 편집` 버튼을 추가했습니다. 실행 중 어느 탭을 보고 있어도 바로 게임 오버레이 편집 모드로 들어갈 수 있습니다.
- 오버레이 편집 모드에서 `투명-/투명+`, `글자-/글자+` 버튼을 박스에 직접 표시합니다. 기존 드래그 이동, 우하단 크기 핸들, 휠 단축키도 유지합니다.
- 버전과 GitHub Release 태그를 `1.6.2` / `v1.6.2`로 올렸습니다.
