# RoiLingo 1.7.0

## Compact UI

- 시작 화면을 작은 컨트롤러 형태로 축소했습니다. 기본 작업은 `대상 → ROI → 시작` 세 단계입니다.
- `오버레이`, `번역창`, `설정`만 보조 버튼으로 남겼습니다.
- OCR/웹 번역/API/기록/로그 등 기존 상세 기능은 `설정`을 눌렀을 때만 큰 패널로 열립니다.
- 설정 패널에서 `닫기`를 누르면 설정/비밀키를 저장하고 다시 컴팩트 크기로 돌아옵니다.
- 고급 설정이 열린 상태에서 `시작`을 눌러도 자동으로 컴팩트 화면으로 복귀합니다.
- 별도 실시간 번역 기록 창은 v1.7 업그레이드 시 자동 열림을 기본 해제했습니다. 필요하면 `번역창` 버튼으로 언제든 다시 열 수 있습니다.

## Lighter startup

- 시작 시 Papago/Google/DeepL WebView2를 미리 초기화하지 않습니다. 실시간 번역을 시작하거나 웹 번역 테스트를 실행할 때만 지연 초기화합니다.
- 기존 ROI, 번역기, API, 기록 기능은 제거하지 않고 고급 설정 안으로 이동했습니다.

## Compatibility

- 설정 스키마를 6으로 올렸으며 기존 ROI와 번역 설정은 유지합니다.
- 프로젝트 버전과 GitHub Release 태그를 `1.7.0` / `v1.7.0`으로 올렸습니다.
