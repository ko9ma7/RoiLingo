# RoiLingo 1.9.0 변경 내역

## 핵심 수정

- 기본 원문 언어를 `자동 감지`로 변경했습니다.
- 기본 OCR 언어를 `eng+kor`로 변경해 영어+한국어가 한 ROI에 동시에 보이는 화면을 정상적으로 읽도록 했습니다.
- 메인 작은 창에 `OCR: 영+한` 버튼을 추가했습니다. 클릭하면 OCR 언어를 여러 개 동시에 선택할 수 있습니다.
- 영어/한국어/일본어/중국어/유럽 주요 언어/동남아 언어 등을 Tesseract 다중 언어팩으로 조합할 수 있습니다.
- `혼합 언어 스마트 처리`를 추가했습니다.
  - 목표 언어가 한국어인데 `Any good ideas? 좋은 생각 있어?`처럼 영어+한국어가 같이 잡히면 한국어 부분은 번역기에 다시 보내지 않습니다.
  - 영어 부분만 추출해 자동 언어 감지 번역 후 한국어 결과를 표시합니다.
  - ROI가 이미 목표 언어만 포함하면 불필요한 재번역/오버레이를 생략합니다.
- 번역 캐시를 `translation-cache-hybrid-v7.json`으로 분리해 이전 잘못된 OCR/번역 캐시의 영향을 차단했습니다.
- Web/API/LibreTranslate 경로는 모두 동일한 `원문 자동 감지 → 목표 언어 고정` 계약을 사용합니다.

## 기본 권장 설정

```text
원문: 자동 감지
OCR: 영어 + 한국어 (eng+kor)
번역: 한국어
방식: 무료 · Web 3종 교차검증
혼합 언어 스마트 처리: ON
```

게임에 일본어도 함께 나오면 `OCR` 버튼에서 일본어를 추가해 `eng+kor+jpn`으로 사용할 수 있습니다.

## 주의

Tesseract는 번역기처럼 이미지에서 언어를 완전히 자동 판별해 모델을 즉석에서 선택하는 방식이 아닙니다. RoiLingo 1.9는 사용자가 선택한 여러 OCR 언어팩을 **동시에** 로드해 다국어 화면을 읽고, 번역 단계는 `자동 감지`를 사용할 수 있도록 분리했습니다. 필요한 언어만 2~4개 정도 선택하는 것이 속도/정확도 균형에 좋습니다.
