# RoiLingo 2.0.0 변경 내역

이번 버전은 기능 수보다 실사용 안정성과 반응 속도를 우선해서 캡처→ROI→OCR→번역→오버레이 흐름을 다시 정리했습니다.

## 핵심 수정

- **가려진/비활성 대상 창 캡처**: `PrintWindow(PW_CLIENTONLY | PW_RENDERFULLCONTENT)`를 우선 사용합니다. 대상 창이 다른 창에 가려져도 화면 좌표 복사에 의존하지 않습니다. 화면 복사는 대상 창이 실제 전면일 때만 최후 폴백으로 사용하므로, 가린 다른 프로그램을 OCR하는 오동작을 막습니다.
- **창 이동과 ROI 분리**: ROI는 계속 대상 창 client 영역의 정규화 좌표(0~1)로 저장됩니다. 창 위치가 바뀌어도 ROI는 그대로 따라갑니다.
- **기존 ROI 직접 편집**: ROI 편집 화면에서 기존 박스를 클릭해 이동하고, 파란 우하단 핸들로 가로/세로 크기를 독립적으로 조절할 수 있습니다. Delete/Backspace 또는 `선택 삭제`로 선택 ROI만 삭제합니다.
- **오버레이 자유 비율 리사이즈**: 오버레이 너비와 높이를 별도 값으로 저장합니다. 핸들의 가로 이동은 너비, 세로 이동은 높이만 바꿉니다. 더 이상 세로 드래그가 글자 크기로 연결되지 않습니다.
- **빠른 시작**: 시작 버튼에서 WebView2 3개 전체 초기화와 Tesseract 언어팩 전체 검사를 기다리지 않습니다. 감시를 먼저 시작하고 OCR 모델/WebView는 첫 사용 또는 시작 후 백그라운드 warm-up으로 준비합니다.
- **이벤트 우선 처리**: EventMode ROI를 일반 ROI보다 먼저 OCR하며, EventMode는 빠른 OCR 경로(SparseText 1회)를 사용합니다.
- **응답 지연 감소**: 기본 화면 검사 간격 150ms, 안정화 100ms, Provider 대기창 2.5초로 조정했습니다. 번역은 기존 latest-wins 큐를 유지합니다.
- **UI 다국어 메뉴**: 작은 메인 창에서 한국어 / English / 日本語 / 简体中文 UI를 바로 바꿀 수 있습니다. 주요 메뉴, 탭, 언어명, 번역 방식 표시가 즉시 갱신됩니다.
- **기존 무료 Web 교차검증 + API/로컬 유지**: Papago/Google/DeepL Web 교차검증, Papago/Google/DeepL API, LibreTranslate/Argos 호환 경로는 그대로 유지합니다.
- **새 캐시 세대**: `translation-cache-hybrid-v8.json`을 사용해 이전 버전의 잘못된 결과가 새 런타임에 섞이지 않도록 했습니다.
- **GitHub 업로더 갱신**: 태그/Release를 `v2.0.0`으로 갱신하고 기존 원격 main 기록을 보존하는 게시 절차를 유지합니다.

## 캡처 제한

`PrintWindow`는 대부분의 일반 Win32/Chromium 창에서 가려진 상태 캡처에 유용하지만, 일부 GPU 전용/보호된 게임 창은 애플리케이션 자체가 off-screen 렌더링을 제공하지 않아 검은 프레임을 반환할 수 있습니다. 이 경우 RoiLingo는 다른 창의 화면을 잘못 OCR하지 않고 캡처 실패로 처리합니다. 최소화된 창 역시 앱에 따라 렌더링이 중단될 수 있습니다.
