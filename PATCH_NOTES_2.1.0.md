# RoiLingo 2.1.0

## 핵심 수정

### 순간적으로 나타났다 사라지는 문구 보존

v2.0은 ROI가 다시 비면 이전 번역을 지우거나, 새 OCR이 생기면 이전 번역 표시를 먼저 제거하는 경로가 있었습니다. 게임 이벤트/관리자 공지처럼 짧게 나타나는 텍스트에는 맞지 않는 동작이었습니다.

v2.1은 ROI 변화 구간마다 가장 글자다운 프레임을 메모리에 스냅샷으로 보존합니다. 화면에서 원문이 사라져도 그 스냅샷의 OCR과 번역을 끝까지 처리합니다.

- 일반 ROI: 최대 4개의 고유 번역 작업을 보존
- 이벤트 ROI: 최대 12개의 고유 번역 작업을 보존
- 무제한 큐는 만들지 않음
- OCR이 확정된 순간 `CAPTURE` 로그를 즉시 파일에 저장
- 번역 성공 결과는 기존 JSONL 번역 기록에 자동 저장
- 번역 실패 시에도 OCR 원문은 실행 로그에 남음

### 오버레이가 원문 소멸과 함께 사라지지 않음

완료된 번역은 기본 20초 유지합니다. 이벤트 ROI는 최소 30초 유지합니다. ROI별 상세 설정에서 0초를 선택하면 다음 번역으로 교체될 때까지 계속 표시합니다.

### 오버레이 리사이즈 재구현

기존 코드는 리사이즈 DragDelta 중 `Render()`를 호출해 드래그 중인 핸들 자체를 다시 생성했습니다. 이 때문에 드래그가 끊기거나 가로/세로가 원하는 대로 조절되지 않는 느낌이 날 수 있었습니다.

v2.1은 드래그 중 전체 UI를 재생성하지 않습니다.

- 왼쪽 위 파란 이동바: 위치 이동
- 오른쪽 파란 변: 가로 너비만 변경
- 아래 파란 변: 세로 높이만 변경
- 우하단 파란 핸들: 가로/세로 동시 변경
- 실제 Width/Height 값을 ROI별 저장
- 번역 결과가 아직 없어도 편집 모드에서는 미리보기 박스를 표시

### 번역 파이프라인

기존 latest-wins 방식 대신 작은 bounded FIFO를 사용합니다. 화면이 빠르게 바뀌어도 이미 캡처한 고유 문장은 순서대로 번역/기록하되, 큐 상한을 둬 과도한 지연과 메모리 증가를 막습니다.

## 호환성

기존 ROI 좌표, 번역 Provider, API/웹 설정, 기록 파일을 유지합니다. 설정 스키마는 10으로 올라가며 새 오버레이 Width/Height/Hold 값만 자동 추가됩니다.
